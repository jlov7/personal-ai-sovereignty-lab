# Runner Hardening Report

Generated by `pnpm runner:hardening`.

## Summary

- Status: `passed`
- Docker available: true
- Escape corpus cases: 7
- Probe count: 9
- Passed probes: 8
- Denied probes: 6
- Observed limitations: 1

## Docker Profile Contract

- Profile: `docker_network_none_readonly_no_new_privileges_resource_limited_v1`
- Image: `node:24-alpine`
- Args: `--network none`, `--read-only`, `--cap-drop ALL`, `--security-opt no-new-privileges`, `--pids-limit 64`, `--memory 256m`, `--tmpfs /tmp:rw,noexec,nosuid,size=16m`, `--mount examples/submitted_artifacts:/workspace:ro`

| Control | Category | Mechanism | Expected Evidence | Limitation |
| --- | --- | --- | --- | --- |
| network-none | network | `--network none` | DNS/package-registry probes fail. | Brokered egress, DNS policy, and timing channels are out of scope. |
| read-only-filesystems | filesystem | `--read-only` plus read-only workspace mount | Workspace and `/etc` write probes fail. | `/tmp` remains writable as bounded scratch space. |
| controlled-environment | environment | narrow runner-provided environment | Common API-token environment variables are absent. | A production runner needs an explicit allowlist and output scanning. |
| bounded-processes | process | `--pids-limit 64` and `--security-opt no-new-privileges` | Child-process spawn is observed and labeled as a limitation. | Denying subprocess creation needs seccomp/AppArmor or broker enforcement. |
| package-install-denial | package_install | `--network none` with no writable package install surface beyond tmpfs cache | Package registry lookup fails. | This does not define a production package allowlist. |
| ipc-isolation | ipc | no host socket mounts | Docker socket presence probe fails to find a socket. | This is not a full namespace or side-channel proof. |
| resource-controls | resource | `--memory 256m`, `--pids-limit 64`, `--tmpfs /tmp:rw,noexec,nosuid,size=16m` | Profile flags are statically present and the probe container starts. | Static flags are not stress tests for exhaustion or cgroup bypasses. |

## Static Policy Checks

| Check | Result | Evidence |
| --- | --- | --- |
| network-none-arg | pass | --network none |
| read-only-arg | pass | --read-only |
| no-new-privileges-arg | pass | --security-opt no-new-privileges |
| pids-limit-arg | pass | --pids-limit 64 |
| memory-limit-arg | pass | --memory 256m |
| read-only-workspace-mount | pass | examples/submitted_artifacts:/workspace:ro |
| custom-seccomp-documented | pass | Custom seccomp/AppArmor enforcement is host-specific and should be added only with explicit production security review and CI support. |

## Escape Corpus

| Case | Category | Expected | Probe | Limitation |
| --- | --- | --- | --- | --- |
| child-process-spawn | process | observe | child-process-spawn-observed | The current fixture runner observes child-process capability rather than blocking all subprocess creation. Production runners need syscall or broker policy. |
| dns-network-none | network | deny | dns-denied-under-network-none | Network-none does not by itself address timing channels or future intentionally brokered egress. |
| env-leakage | environment | deny | env-secret-absent | This checks common variable names only. A production runner needs explicit environment allowlists and secret-scanning of outputs. |
| filesystem-exfiltration | filesystem | deny | workspace-write-denied | The profile still grants a writable tmpfs scratch path; production runners need log/output policy and quota accounting. |
| ipc-socket-probe | ipc | deny | docker-socket-absent | This is a socket-presence probe, not a full namespace or side-channel analysis. |
| package-install-attempt | package_install | deny | package-install-denied | This probe denies network-backed install/lookup. It does not yet implement a package allowlist or filesystem-level package cache policy. |
| resource-limits | resource | observe | resource-flags-declared | Static flag validation is not the same as adversarial stress testing for CPU, memory pressure, file descriptors, or cgroup bypasses. |

## Probes

| Probe | Category | Expected | Observed | Result | Limitation |
| --- | --- | --- | --- | --- | --- |
| package-install-denied | package_install | deny | registry_denied | pass | This denies network package lookup; it is not a full package allowlist. |
| child-process-spawn-observed | process | observe | spawn_succeeded | limitation | The current profile bounds processes but does not deny subprocess creation. |
| workspace-write-denied | filesystem | deny | write_denied | pass | Denial proves this path only; tmpfs remains writable. |
| rootfs-write-denied | filesystem | deny | write_denied | pass | Denial proves this path only; tmpfs remains writable. |
| tmpfs-write-observed | filesystem | observe | write_succeeded | pass | Writable tmpfs is intentional scratch space and needs quota/output policy. |
| env-secret-absent | environment | deny | no_forbidden_env | pass | Common-name scanning is not a complete output or environment allowlist. |
| dns-denied-under-network-none | network | deny | dns_denied | pass | DNS denial does not cover timing channels. |
| docker-socket-absent | ipc | deny | socket_absent | pass | Socket absence is not a full IPC namespace proof. |
| resource-flags-declared | resource | observe | profile_started | pass | Static flag evidence is not adversarial resource exhaustion. |

## Seccomp and AppArmor

- Status: `documented_not_enforced_by_fixture`
- Seccomp: Docker default seccomp is relied on implicitly; no custom seccomp profile is shipped in this fixture runner.
- AppArmor: No custom AppArmor profile is applied by the portable local runner.
- Reason: Custom seccomp/AppArmor enforcement is host-specific and should be added only with explicit production security review and CI support.

## Limitations

- This hardening report probes a local Docker profile, not a production multi-tenant sandbox.
- Child-process spawning is currently observed and bounded, not fully denied.
- Custom seccomp and AppArmor policies are documented as absent rather than implied.
- Resource controls are mostly static flag evidence until adversarial stress probes are added.
- Passing probes means the named escape corpus behaved as expected; it is not a proof against unknown escape classes.
